<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- fill color -->
    <solid android:color="@color/white"/>
    <!-- corners -->
    <corners android:radius="10dp"/>
    <!-- stroke color -->
    <stroke
        android:width="1dp"
        android:color="@color/back_btn"/>
</shape>
